# AMIT RAG Chatbot

A retrieval-augmented generation chatbot with an API layer, embeddings, and vector retrieval.

## Dataset and generated artifacts
The source PDF and generated retrieval artifacts are stored separately. Add the Google Drive dataset link here after upload.
